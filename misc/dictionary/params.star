param("word", description="The word to lookup", default="")

param("show", description="The info to show", default="basic")

param("options-show", description="Options for show", type=LIST, default=["basic", "summary", "detail", "custom"])

